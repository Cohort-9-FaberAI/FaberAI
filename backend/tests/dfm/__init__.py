"""Package marker for the DFM tests — see ``tests/__init__.py``.

``tests/ai_eval/runner.py`` imports ``tests.dfm.conftest`` for its geometry
fixtures, so this subpackage has to resolve by name too.
"""
